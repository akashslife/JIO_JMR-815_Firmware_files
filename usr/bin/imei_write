if [ $# -eq 0 ] ; then
	echo "usage : imei_write (IMEI VALUE)"
else
	if [ `echo $1 |wc -m` -eq 16 ];then
		/etc/ue_lte/at.sh at%setcfg=\"debug_imei\",\"$1\" 10
		sync
	else
		echo "IMEI NOT INVALID"
	fi
fi
